const serverBaseURL = "https://express-server-api.vercel.app/api/dividend";
// eslint-disable-next-line
const serverDevURL = "http://localhost:5000/api/dividend";

export const serverURL = serverBaseURL;